{
  "name": "shadow-intel-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web-dashboard",
    "mobile-verify",
    "backend"
  ],
  "scripts": {
    "build:web": "npm run build --workspace=web-dashboard",
    "build:mobile": "npm run build --workspace=mobile-verify"
  }
}
